# Fase 0 - Baseline e Congelamento

Data de congelamento: 2026-03-31
Escopo: `percival_vision_mcp`

## Objetivo

Estabelecer baseline técnico antes da refatoração para rastrear regressões e validar aderência ao padrão do monorepo.

## Inventário Congelado

- Legacy runtime MCP manual em `src/server.py` com `mcp.server.Server`.
- Ferramentas legadas identificadas:
  - `list_available_vision_models`
  - `analyze_image`
  - `describe_image`
  - `identify_objects`
  - `read_text`
- Documentação e exemplos com resquícios de Ollama.
- Suíte de testes legada inconsistente com o código atual.

## Principais Lacunas Congeladas

- Sem FastMCP padrão com `main.py` unificado.
- Sem contrato estruturado para nanobot (`ok/data/error/meta/request_id`).
- Sem `get_nanobot_profile`.
- Sem runtime unificado `stdio|sse|streamable-http`.

## Critérios de Aceite para saída da Fase 0

- Baseline documentado e versionado.
- Snapshot machine-readable criado (`baseline_snapshot.json`).
- Lista de compatibilidade mínima definida para migração:
  - manter nomes das 5 tools legadas.
  - suportar inicialização via `uv run ... python main.py --mode stdio`.
  - preservar compatibilidade temporária com `python -m src.server`.

## Compatibilidade Mínima Definida

- Nomes de tools legadas preservados para evitar quebra de orquestração no nanobot.
- Erros e respostas passam a ser estruturados, mantendo `legacy_text` para transição gradual.

